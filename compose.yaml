version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
      - 2345:2345
    volumes:
      - ./:/app/
    security_opt:
      - apparmor:unconfined
    cap_add:
      - SYS_PTRACE